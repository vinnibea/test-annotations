import React from "react";
import { Button } from "../Button/Button";
import "./Header.scss";

export const Header = () => {
  return (
    <header className="App-header">
      <h1 className="App-header">Here goes the file name</h1>
      <Button />
    </header>
  );
};
